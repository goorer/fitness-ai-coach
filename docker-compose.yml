services:
  backend:
    build:
      context: ./backend
    container_name: fitness-ai-coach-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
      - ./backend/data:/app/data
    environment:
      - PYTHONUNBUFFERED=1